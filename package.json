{
  "name": "package",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    
    "dev": "node server/index.js",
    "seed": "cd server && node seed.js",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci",
    "start": "cd server && npm ci && npm run start",
    "lint": "eslint .",
    "preview": "vite preview",
    "render-build": "cd client && npm ci && npm rebuild --platform=linux && npm run build",
    "railway-build": "cd client && npm ci && npm run build"
  },
  "dependencies": {
    "concurrently": "^9.0.1",
    "multer": "^2.0.2",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.1",
    "concurrently": "^9.2.0",
    "eslint": "^9.33.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "globals": "^16.3.0",
    "vite": "^7.1.3"
  }
}
