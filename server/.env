# Server Configuration
PORT=4000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# CORS Configuration for development
CORS_ORIGIN=http://localhost:5173

# JWT Configuration
JWT_SECRET=dev_jwt_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# PostgreSQL Configuration (Development)
PGHOST=localhost
PGPORT=5432
PGDATABASE=tupad_validator
PGUSER=postgres
PGPASSWORD=121693

# Development Database URL (fallback)
DATABASE_URL=postgresql://postgres:121693@localhost:5432/tupad_validator

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Development Flags
DEBUG=true
VERBOSE_LOGGING=true

# Database Connection Pool Settings (for development)
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_IDLE_TIMEOUT=30000